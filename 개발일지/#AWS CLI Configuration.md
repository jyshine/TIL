# AWS CLI Configuration


```
$ aws --version
$ aws configure list
$ vi ~/.aws/credentials


[default]
aws_access_key_id = key_id
aws_secret_access_key = secret_asses_key

[name]
aws_access_key_id = key_id
aws_secret_access_key = secret_asses_key

$ aws sts get-caller-identity

```




#aws
