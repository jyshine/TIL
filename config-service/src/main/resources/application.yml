server:
  port: 8888

spring:
  application:
    name: config-service
#  profiles:
#    active: native

  cloud:
    config:
      server:
        git:
          uri: https://github.com/jyshine/config-repo.git
#        native:
#          search-locations: file://${user.home}/dev/git/native-config


